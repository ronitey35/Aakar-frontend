import React from "react";

const Aside = ({ name, age }) => {
  return (
    <div className="text-3xl">
      <h1>{Props.name}</h1> z<h1>{Props.age}</h1>
      <h3>{name}</h3>
      <h3>{age}</h3>
    </div>
  );
};

export default Aside;
